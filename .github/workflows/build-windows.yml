name: Build Windows Artifact
on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Zip Files for Windows
        run: Compress-Archive -Path "*" -DestinationPath "twilight-waterfall-f22c-Windows.zip"
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: twilight-waterfall-f22c-Windows
          path: twilight-waterfall-f22c-Windows.zip
